@model IEnumerable<Repository.Origin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Origin1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.State1.StateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Origin1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.State1.StateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrID })
        </td>
    </tr>
}

</table>
